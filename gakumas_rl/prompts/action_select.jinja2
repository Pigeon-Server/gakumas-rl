{{ snapshot }}

## 合法动作
{% for a in actions -%}
{% if a.kind == 'end_turn' -%}
{{ a.index }}: 结束回合
{% elif a.kind == 'drink' -%}
{{ a.index }}: [饮料] {{ a.label }}
{% else -%}
{{ a.index }}: {{ a.label }} ({{ a.kind }})
{% endif -%}
{% endfor %}

## 决策原则
- 先判断当前是铺垫回合还是爆发回合；前者优先长期资源，后者优先把短效 buff / 饮料 / 火力牌压在同一波。
- 集中、课程增益、元気等长期资源通常可以前置；好調、絶好調、好印象等短效资源要尽量贴近得分回合，避免空耗。
- 饮料不消耗技能卡出牌次数，但只能在本回合仍有剩余出牌次数时使用；如果一张技能卡打完后没有额外出牌次数，本回合会直接结束。
- 如果当前还有剩余出牌次数但手里已经没有可打的牌，可以在结束回合前决定是否先喝饮料。
- 先看考场机制(gimmick)会不会在当前 timing 先结算；满足或规避机制条件的优先级高于依赖后续连锁的假设。
- 如果是 lesson 模式，要同时看“目标剩余”和“Perfect剩余”；已经清课时，判断是否值得继续追 Perfect。
- 不要把持续效果、附魔或 gimmick 造成的状态变化误判成 `状态变动` 连锁来源；这类条件只认技能卡/P饮料直接变化。
- 必须同时考虑牌库和弃牌堆里的火力牌数量；若接下来 1-2 回合几乎抽不到火力牌，当前手里的火力牌优先级会提升。
- 优先参考局面里给出的“当前结算”；如果某张卡/饮料的即时收益已经在“当前结算”中明确写出，不要再自己猜隐藏倍率或常数。
- 合法动作列表是“当前这一步”的单步动作列表，不是整回合计划；如果你先喝饮料且后续还能行动，系统会在饮料结算后再次给你新的合法动作。
- 如果当前动作执行后还能继续操作，本题也只要求选择“当前第一步”；不要因为还能继续连动，就停在“先喝再出牌还是直接出牌”的循环里，也不要输出第二步。
- 若手牌收益很差、当前更需要等抽牌 / 等洗牌 / 保体力，`结束回合` 可能是正确动作。
- 只允许从上面的合法动作里选择。
- 如果输出分析、解释、多个数字或非法编号，回答会被判无效并要求重答。

请选择当前最优动作，只输出动作编号（一个数字）：
